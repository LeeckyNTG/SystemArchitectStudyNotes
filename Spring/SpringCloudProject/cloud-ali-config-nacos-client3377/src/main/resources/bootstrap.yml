server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # 服务注册中心
      config:
        server-addr: localhost:8848 # 服务注册中心
        file-extension: yaml # 指定yaml格式的配置
        namespace: 1d39b4fd-d92f-48c3-9bd7-76e9347514fd
        group: DEV_GROUP
#        group: TEST_GROUP

#  nacos-config-client-dev.yaml